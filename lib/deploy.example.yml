# no trailing slashes on any directories
local:
  name: local
  hostname: localhost
  local: true
  db_name:
  db_user:
  db_pass:
  db_backups_dir: db_backups
  # start from User path
  deploy_path: ~/path/to/local/site

production:
  name: production
  stage: prod
  hostname:
  user:
  port:
  branch: master
  # start from ssh login path
  deploy_path: path/to/remote/site
  db_name:
  db_user:
  db_pass:
  db_backups_dir: db_backups

stage:
  name: stage
  stage: stage
  hostname:
  user:
  port:
  branch: develop
  # start from ssh login path
  deploy_path: path/to/remote/site
  db_name:
  db_user:
  db_pass:
  db_backups_dir: db_backups
